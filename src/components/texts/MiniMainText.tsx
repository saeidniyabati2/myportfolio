interface props {
  title: string;
  autorName: string;
  countComment: number;
  date: string;
}

export const MiniMainText: React.FC<props> = ({
  title,
  autorName,
  countComment,
  date,
}) => {
  return <div>df</div>;
};
