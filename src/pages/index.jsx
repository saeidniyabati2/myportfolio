import ContainerBlock from "../components/ContainerBlock";
import Hero from "../components/Hero";


const Home = (props) => {
  return (
    <ContainerBlock >
      <Hero />
    </ContainerBlock>
  );
};

export default Home;
